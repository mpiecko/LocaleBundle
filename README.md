README
======

LocaleBundle
-----------------

This is just a very basic example of how to set the locale based on the user agent preferences.
The locale is then saved in the session (as _locale) and used in following requests.